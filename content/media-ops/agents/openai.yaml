interface:
  display_name: "跨平台自媒体运营"
  short_description: "生成多平台差异化内容，并在明确授权后发布"
  default_prompt: "Use $media-ops to prepare verified platform-specific content and publish it only after I explicitly approve the exact account and final draft."
