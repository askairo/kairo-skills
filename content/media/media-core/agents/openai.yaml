interface:
  display_name: "Media Core"
  short_description: "Produce verified content assets and distribution targets"
  default_prompt: "Use $media-core to fulfill a bounded production request, verify source and media, create a ready content asset, and prepare platform targets without publishing."
