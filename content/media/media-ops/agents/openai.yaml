interface:
  display_name: "跨平台自媒体运营"
  short_description: "补充内容供给并受控发布多平台内容"
  default_prompt: "Use $media-ops to scan due targets; when supply is empty, ask media-loop for a bounded production request, have media-core prepare one verified asset, then publish through the platform skill if all gates pass."
