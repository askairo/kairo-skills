---
name: xiaohongshu
description: 小红书平台策略与发布子技能：对已核验内容资产完成小红书平台筛选、图文改编、受控发布与核验。Use when Codex needs to adapt, publish, or review content specifically for Xiaohongshu. Source retrieval, account data, and account-specific visual specifications belong to media-core external records and local configuration. Do not use for credentials or unverified claims.
---

# Xiaohongshu

本技能处理小红书平台筛选、图文改编、发布和核验。`media-core` 负责来源发现、证据、版权、去重和媒体资产；`media-ops` 传入已核验资产、目标账号和本机策略。本技能不获取来源，不维护账号、来源组、资源路径或账号专属视觉规范。

文本改编前必须读取 [copy-framework.md](references/copy-framework.md)。视觉质量不能替代文字的点击、读完和收藏理由；标题、开头、正文结构、结尾和标签必须作为一个整体校验。

若 `media-ops` 或 `media-core` 传入 `editorialContextRefs` / `editorialFrameworkRef`，先读取对应的主题编辑框架，再进行小红书平台化改编。主题框架提供事实边界、人物原意、栏目知识和内容门禁；账号专属封面规范只能从本机配置或外部账号文档的 `coverSpecRef` 读取，不得写回本技能或主题框架。

## Assess platform fit

- 根据已核验资产的读者问题、信息密度、图片/截图权限和账号目标，判断其是否可形成搜索可见、具有收藏价值的图文；不把其他平台热度直接当作选题结论。
- 记录适配所需的结构、卡片数量、封面承诺和平台风险；来源与媒体权限以内容资产记录为准。

## Adapt content

- 交付 3 个标题候选：明确主题、读者收益或认知差异，不用虚假夸张词。
- 标题必须在发布前校验为不超过 20 个字符（包括标点和 emoji）；编辑器出现 `超过 20`、`X / 20` 或类似校验提示时，先压缩标题再进入发布动作。
- 开头用 2—3 句完成“具体问题/反差 → 人物的一个判断或事实 → 这对读者有什么用”；禁止先写平台背景、账号背景或空泛赞美。
- 正文只围绕一个核心问题展开，按“事实/原意 → 核心判断 → 解释依据 → 可带走的启发”推进；短段落、小标题和列表优先。
- 优先提供可收藏的判断清单、思考步骤、对比或避坑建议；每张卡片只承载一个信息单元，卡片文字与正文结论一一对应。
- 人物观点系列可采用“上下文一句话 → 人物原话/原意 → 理念解释 → 一句引导”的短结构；人物、来源披露与领域限制均以内容资产和账号外部策略为准。
- 封面承诺必须由正文兑现；来源、日期、事实与个人判断清晰分开。
- 标签围绕真实主题，不堆叠无关热门词；商业关系、素材来源和必要披露必须明确。

## Media and review

配图、截图、信息卡和视频必须真实、清晰、具备使用权限；不为凑九宫格重复或拼接无关图片。发布前检查标题、封面、正文、来源区、标签、账号身份和发布按钮；发布按钮只点击一次并等待明确结果。

- 发布前重新读取编辑器中的最终标题、正文和媒体状态，确认标题长度校验已通过。
- 若 `media-ops` 传入 `browserProfileRef`，先确认当前 Chrome Profile 已完成路由，并核对小红书公开账号身份；Profile 路由缺失或账号不一致时停止。
- 点击发布后只接受明确的成功提示、已发布状态、帖子 URL 或账号时间线新内容；如果页面仍停留在编辑器、出现校验错误或状态不明确，记录为“发布未确认”，并在运行记录中保留具体页面信号。
- 只有当本机执行策略明确配置 `retry-on-failure` 时，`publish_failed` 或 `publish_unconfirmed` 才允许在后续运行重试；每次最多点击一次，重试次数遵守配置上限（未配置时默认不重试）。账号不匹配、事实/版权门禁失败、安全验证和验证码不可重试。
- 发布未确认时，不得把候选写入已发布清单或推进来源游标；达到重试上限后暂停该候选，避免重复发布。

将发布结果交给 `media-ops` 完成外部运行记录和队列回写；回写成功后由 `media-ops` 统一关闭本次运行创建的素材、编辑器和结果页。发布未确认、草稿待修复或用户要求保留页面时，保留必要的交接页，不得先关页。
