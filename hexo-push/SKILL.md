---
name: hexo-push
description: 读取 Clippings 目录中的最新文章，转换为 Hexo 博客文档并自动发布。支持发布前预览、分类标签确认、去重更新和自动部署重试。
---

# Hexo Clipping 发布工具

自动将 Clippings 目录中的最新文章转换为 Hexo 博客格式并发布。

## 职责边界

- Agent 负责内容理解：阅读全文，判断是否需要翻译、润色、精简，生成自然的标题、摘要、分类和 tags。
- 脚本负责机械发布：读取 Markdown、生成 Hexo front matter、去重更新、写入文件、执行 git 和 Hexo deploy。
- 用户只需用自然语言确认分类、标签和发布意图。
- 仓库源码才是长期维护源；各 Agent 用户目录只是同步安装目标。

## 推荐流程

1. 找到最新的 Clippings 原文。
2. 判断主题、标题、摘要和分类。
3. 生成 Hexo 文章并预览。
4. 确认标签后发布。
5. 如重复文章已存在，按规则更新。

## 注意

- 不要把原文直接整篇搬到博客里。
- 发布前先让用户确认分类和标签。
- 需要时支持去重更新和重试。
